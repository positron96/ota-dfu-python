[tool.poetry]
name = "ota-dfu-python"
version = "0.1.0"
description = "A Device Firmware Update (DFU) server for Nordic nRF51/nRF52 systems."
authors = ["Your Name <your.email@example.com>"]
license = "Apache License"

[tool.poetry.dependencies]
python = "^3.8"
bleak = "^0.20.2"
intelhex = "^2.3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"